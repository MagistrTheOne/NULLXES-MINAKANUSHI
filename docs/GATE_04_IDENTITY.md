# Gate 04 — SelfModel + Authority + Persona

Identity is runtime structure. Not a prompt. Not a chatbot.

```text
MINAKANUSHI = World Model + Self Model + Authority + Experience + Embodiment
Persona / Voice / Face sit above that and do not drive belief or policy.
```

Short name: **MINA**. Full: **MINAKANUSHI**. Organization: **NULLXES**.
No email, no contact fields, no "you are MINAKANUSHI" text objective.

SelfModel is a passport (dataclass). Persona is presentation
(feminine-presenting interaction property, not `gender=female` as cognition).
Authority modes: AUTONOMOUS, ADVISORY, DIRECTED, MANUAL, SAFE_HOLD.
`policy_enabled=false` disables ActionPolicy selection only.

v0.2 Identity Initialization stamps this passport into `*.mina`. It is not
`identity_loss` and not a train run. Authority is a gate after ActionIntent.
See `docs/MINA_TRAINING_V02.md`.
