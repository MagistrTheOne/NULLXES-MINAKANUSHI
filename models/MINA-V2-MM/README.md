# MINA V2 — Multimodal Cognitive Layer (experiment pack)

```text
EXPERIMENT. NOT THE FOUNDATION.

NULLXES MINAKANUSHI
short name: MINA
product: MINA V2 — Multimodal Cognitive Layer
```

Not “MINA Vision”. Vision is one organ.

Do not construct `minakanushi_6_8b` from this folder. Organs run on `configs/organs_cpu.yaml` (cpu_dev scale). Frozen 6.8B is the future cognition substrate after v0.2 gate, IdentityBound, resume, Yunmu, Gate 9+.

Canon: `docs/experiments/MINA_V2_MULTIMODAL.md`

```text
Cosmos:  What is likely to happen? Draw / generate it.
MINA:    What exists? How sure am I? What changed? Which action is allowed?
```

Not a VLA (`camera → ViT → action`). Not a mega token transformer. Not NVIDIA Cosmos weights.

```text
organs → MinaUnit Space → 1.0 State Constructor / DWC / Belief
       → SceneBranch (relations, risk, uncertainty)
       → ActionIntent (1.0 kernel + authority, not this pack)
```

| | |
|---|---|
| Identity | `identity.yaml` |
| Organ contract | `architecture.yaml` |
| Runnable cfg | `configs/organs_cpu.yaml` (`latent_dim: 64`) |
| Code | `src/mina_v2/` |
| Spec | `docs/experiments/MINA_V2_MULTIMODAL.md` |

Import: `mina_v2` (package root `src/`). PWM is forbidden. Language does not call ActionPolicy.
